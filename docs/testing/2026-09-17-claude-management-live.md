# Claude Code 退出管理实机测试

2026-09-17，在本机真实 Claude Code 目录调用应用同一 Rust 核心服务测试；未进行原生窗口点击自动化。

测试前 Claude skill 目录无已安装 skill、无启用分组。创建两个临时分组、两个 Hub 测试 skill 和一个原地手动测试 skill（收录进 Hub 后测试还原）。

全部通过：
- 启用 A，关闭保留手动 skill 时暂时停用手动项。
- A 切换 B，撤销 A 副本并部署 B。
- 修改 B 副本后关闭管理被拒绝，文件及开关状态保留。
- 关闭管理撤销部署，恢复手动项与原目录；Hub 保留独立副本。
- 重新读取磁盘配置后仍保持关闭；关闭状态拒绝启用分组。
- 重新开启不自动启用分组；再次启用和关闭成功。
- 开启保留手动 skill 时手动项保持启用。
- 正常停用分组后关闭、重复关闭、再次开启均成功。
- 退出管理后 Claude settings.json 的 JSON 内容与测试前一致，其他 Agent 分组保持不变。

测试结束删除临时 skill、还原 Studio 配置与备份目录及 Claude 原生设置；逐项内容比对通过。Codex 原生配置与语雀 skill 的文件/软链内容校验一致，未修改语雀 skill。

局限：验证当前本机目录状态及以上临时样本，不代表所有历史配置、文件冲突和平台均无缺陷。

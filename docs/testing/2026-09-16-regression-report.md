# 第一批修复后的真实环境回归测试

日期：2026-09-16。平台：macOS。范围：Hub 迁移、副本校验，以及相关全局注册、分组、项目、配置链路。

## 测试方式

- 使用官方 skill-installer 的 Git 稀疏检出模式下载 `openai/skills` 中的 `skills/.curated/gh-fix-ci`，以 `ss-e2e-gh-fix-ci` 安装到本机 Codex skills 目录。公开列表 API 返回 403，ZIP 下载无响应，Git 下载成功。
- 加入独立资源文件、相对软链接和绝对软链接，另创建一个 `ss-e2e-secondary` 样本，检查批量操作不会影响组外 skill。
- 在真实 Tauri debug WebView 中执行测试，调用真实 IPC 命令和本机文件系统；Hub 迁移通过真实菜单及确认对话框操作，并检查列表、状态徽标、导航和设置页。其他业务操作主要通过真实 IPC 调用，不是每个按钮都逐一点击。
- 临时原生测试入口、注入脚本没有保留在产品代码中。运行脚本和初次失败证据保存在 `/private/tmp/skill-studio-e2e-20260916/`；最终原生测试结果见相邻 `2026-09-16-native-results.json`。
- 没有执行下载 skill 内的脚本或发起 GitHub CI 操作。

## 结果

最终原生测试共 29 个检查：28 个通过，1 个原有问题仍失败；整个业务流程执行到清理阶段，没有中途业务异常。

通过的主要链路：

1. 真实样本扫描及界面显示；全局 Copy/Symlink 注册和重复注册。
2. 分组创建、改名、成员去重、排序、应用和移除；组外注册保持不变。
3. 项目直接绑定与分组绑定合并去重，向 Claude/Codex 项目目录写入、更新、移除。
4. `.gitignore` 保留已有规则、增量补齐、幂等；另外使用实际 `git check-ignore` 验证事务锁文件被忽略。
5. 通过界面迁移到 Hub；相对与绝对内部软链接可读；原位置 Copy 注册可用。
6. 迁移同步更新分组、项目、全局和项目副本引用，同时保留副本过期基线。
7. 源变更、仅副本变更、两边同时变更、缺失 `SKILL.md` 的状态区分。
8. 普通更新/移除保护本地修改；显式强制修复可恢复副本。
9. 测试 skill 的 Claude/Codex 原生启停往返；这里只验证当前实现的名称规则和状态回读，不代表 Codex 路径规则兼容问题已修复。
10. 分组、项目、设置页导航；删除元数据、重复卸载、配置备份与恢复。

另通过正式自动化检查：133 项 Rust 测试（含 2 项桌面命令辅助逻辑测试）、40 项前端测试、TypeScript 类型检查、workspace Clippy、前端构建和桌面 debug 编译。

## 本轮发现并修复的回归遗漏

- 新增的持久事务锁文件没有被项目 `.gitignore` 排除。已补齐锁文件及恢复日志的忽略规则，并测试升级已有规则和重复写入。读取已有 `.gitignore` 失败时现在保留原文件并报错，避免覆盖。
- 新的软链接哈希表示未区分文件类型：将软链接替换为内容恰好等于其内部表示的普通文件，会误报为正常副本。已加入类型区分。新增回归用例先复现失败，修复后通过。

## 仍存在的原有问题

这些问题不属于第一批已修复范围，不能把本次测试理解为整个项目已无 bug。

| 问题 | 验证结果 |
| --- | --- |
| 外部编辑 skill 内部文件后不自动刷新 | 真实窗口复现；手动刷新可显示正确状态。原有 watcher 只监听根目录非递归变化，启动时不存在的 Hub 也未被监听。 |
| 外部仓库通过软链接挂入的 skill 不显示 | 原有隔离复现用例仍失败。 |
| Codex `path` 选择器禁用状态识别错误 | 原有隔离复现用例仍失败。 |
| YAML 错误标记被丢弃 | 原有隔离复现用例仍失败。 |
| 项目快速连续勾选丢失早先选择 | 原有前端复现用例仍失败：请求未完成时依次选 A、B，后一次提交仅含 B。 |

这些已知问题的复现脚本没有作为失败用例留在默认测试套件中；输出保存在临时测试目录的 `remaining-rust.txt`、`remaining-ui.txt`。

## 清理与数据保护

- 已卸载两个临时测试 skill，并移除其副本、链接、事务文件、分组及项目记录。
- 已恢复测试前 Codex、Claude 和 Skill Studio 配置的原始字节；原先不存在的配置文件已移除。
- 对测试前的全部 skill 入口逐项比较：文件 SHA-256、权限、目录结构、软链接目标均一致。语雀 skill 的各入口及可解析的真实目录也单独核验，一致。
- 安装器产生的 Python 缓存也已清理。校验结果为 `protectedUnchanged=true`、`configsUnchanged=true`。
- 原有日志保留，正常 debug 程序重新启动；没有提交 Git commit。

未覆盖 Windows/Linux、跨卷迁移、强制断电，以及每个系统文件对话框的人工交互。进程中断恢复和失败回滚由隔离 Rust 测试覆盖。

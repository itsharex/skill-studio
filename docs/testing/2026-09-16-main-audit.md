# 本地 main 相对 origin/main 审核

日期：2026-09-16。已执行 `git fetch origin main`。

- 远程基线：`21b1981`
- 本地审核起点：`6280ad7`（领先 3 个提交，66 个文件）
- 本报告的修复尚未提交。

## 确认的问题与修复

| 级别 | 问题 | 处理 |
| --- | --- | --- |
| P1 | 项目先保存选择、再写入文件；文件写入失败后配置仍显示新选择 | 合并为一个 IPC 和可恢复的文件/配置事务，失败一起回滚 |
| P1 | 项目写入仅新增，取消 skill 或 Agent 选择后旧文件仍生效 | 记录项目部署归属；移除不再选择的部署，校验路径、sidecar 和内容；保留手动文件，拒绝覆盖/删除本地修改 |
| P1 | 恢复备份在配置锁之外先写磁盘，又重复保存；备份版本和运行中分组状态缺少恢复前校验 | 在同一写锁内校验后保存一次；拒绝未来版本以及涉及启用中分组的纯配置恢复 |
| P2 | 重新应用分组跳过已有成员，Hub 更新不能刷新该成员副本 | 检测陈旧副本并事务替换，界面显示“应用修改”入口 |
| P2 | Hub 路径逐字符持久化，中间路径立即影响扫描 | 编辑后显式保存；目录选择也先填入草稿 |
| P1 | Hub 目录切换没有迁移文件，且未阻止目录重叠 | 校验绝对路径与 Agent 目录边界；有运行中分组或原 Hub 非空时拒绝切换，避免丢失管理入口 |

## 简化

- 删除无调用的 `useQueuedDraft`。
- 删除 5 个无调用的旧分组 mutation Hook，以及拆分式项目更新 Hook。
- 去掉项目界面“先保存、再写入”的两阶段调用。
- 备份恢复不再重复落盘。
- 历史共享分组的数据结构和兼容 API 保留，避免破坏已有项目绑定。

## 验证

- Rust workspace：176 通过；1 项联网测试按原标记忽略。
- 前端：70 通过，覆盖项目草稿/离开确认、设置、分组、Hub、安装界面和 IPC 合同。
- TypeScript 类型检查通过。
- Clippy 全 workspace/all-targets，`-D warnings` 通过。
- Renderer production build 通过；存在约 655 kB 主 JS chunk 的体积提示。
- `git diff --check` 通过。
- 新增/补充回归：项目取消选择、手动文件保留、冲突不落盘、本地修改保护、事务中断恢复、分组副本更新、备份校验及恢复、Hub 路径草稿和目录边界。

测试操作使用临时目录，没有修改真实的 `skill-yuque-cli-guide`。

## 边界与后续

- 旧版没有部署归属记录的项目文件不按名称推断归属，也不会自动清除；选择并重新写入后才纳入新记录。
- Hub 目录变更不包含自动迁移；已有内容时明确阻止，而不是静默改变扫描位置。
- 此次没有运行联网 marketplace 测试、原生窗口人工回归或跨平台实机测试；现有模拟与文件系统测试通过不等于所有环境下没有 bug。
- 主 bundle 的体积提示属于性能优化候选，不影响本次正确性检查；未为消除提示引入额外打包配置。

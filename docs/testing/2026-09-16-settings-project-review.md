# 设置页与项目编辑回归（2026-09-16）

## 环境与范围
通过 pnpm dev 启动真实 Tauri debug 应用，开发服务器为 localhost:3000。
真实应用启动日志正常。界面点击验证使用相同前端组件、官方 Tauri mockIPC/mockWindows
和隔离内存数据，通过浏览器完成；原生窗口关闭通过事件模拟测试，
未进行原生窗口端到端点击验证。没有修改用户的真实项目配置或 skill。

## 验证
- Rust workspace：169 项通过。
- 前端：69 项通过，覆盖手动保存、保存失败重试、放弃修改、继续编辑、
  顶部导航与搜索拦截、保存后离开、原生关闭事件模拟、设置控件和恢复确认。
- TypeScript、前端生产构建、git diff --check：通过。
- 浏览器点击：四个设置页签、浅色/深色外观、目录与关于页面布局、
  项目搜索框焦点光圈、未保存切换到设置时弹窗、继续编辑保留勾选、
  点击写入后离开不再弹窗、备份保留数量有效值与上限。
- 临时浏览器测试入口和数据文件已删除，真实 debug 应用继续运行。

## 本轮发现并修复
1. 设置页使用不支持透明度后缀的自定义 border-border-default/60、
   divide-border-default/60，深色模式分隔线显示过亮。
   改为 Tailwind 支持的 border-border/60、divide-border/60，截图复核正常。
2. 备份数量未严格限制整数和上限，清空也会被当作零提交。
   增加非空、整数、0–100 验证；新增回归测试，并在浏览器确认 101 不会保存。

以上为已执行范围，不构成对所有平台及所有异常环境的无缺陷保证。

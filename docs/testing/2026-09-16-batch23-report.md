# 第二、三批修复及完整链路回归

日期：2026-09-16。平台：macOS，真实 Tauri debug 应用。

## 修复结果

- 外部仓库通过软链接挂入的 skill 可发现，同一真实来源的多个别名去重并保留入口信息。解除注册只移除入口，不删除外部源；外部来源不能直接托管。断链、循环和缺少 SKILL.md 的目标保留诊断。
- Codex 启停使用实际 SKILL.md 路径及 frontmatter 中的声明名称，按配置顺序计算 name/path 规则。写入精确路径开关，保留其他同名 skill 的规则；显式启用能够覆盖先前名称禁用。支持普通表数组和内联表数组，保留其他配置。
- 项目和分组选择立即更新本地草稿，串行保存最新选择。写入项目、详情返回等待保存完成；失败保留草稿并提供重试。快速连续勾选不再用过期数据覆盖此前选择。
- YAML 解析错误、缺少结束分隔符的标记和错误内容保留到界面，不再静默丢弃。
- 文件监听覆盖 skill 内部真实目录及外部来源；动态跟随新建 Hub 和目录覆盖配置，不跟随内部循环软链接。
- 实测额外发现导航标题已变化但正文停留旧页的情况。移除页面切换对退场动画完成事件的依赖，保留新页淡入；增加正文切换回归测试。

Codex 规则参考官方实现：<https://github.com/openai/codex/blob/main/codex-rs/config/src/skills_config.rs>。这里处理用户配置，不读取正在运行的 Codex 进程的会话级开关。

## 验证

| 检查 | 结果 |
| --- | --- |
| Rust workspace 全量测试 | 142/142，通过 |
| 前端测试 | 44/44，通过 |
| TypeScript 类型检查 | 通过 |
| workspace Clippy（警告视为错误） | 通过 |
| 前端生产构建、桌面 debug 编译 | 通过 |
| 真实桌面链路 | 36/36，通过 |
| git diff --check | 通过 |

原生逐项结果见 `2026-09-16-batch23-native-results.json`。本轮复用了此前从官方 openai/skills 下载的 gh-fix-ci 样本，并创建专用 ss-e2e 前缀的外部链接、断链、循环及无效 YAML 夹具，没有执行下载 skill 的脚本。

覆盖扫描、真实界面诊断、外部别名卸载、同名 skill 的独立启停、Copy/Symlink 注册及幂等、分组、项目、快速连续勾选后写入、gitignore、Hub 菜单迁移、引用更新、过期/修改/冲突/损坏副本保护、强制修复、目录覆盖后的自动刷新、备份恢复及卸载清理。前端组件测试另覆盖人为延迟写入、保存失败、重试及写入项目等待。

测试通过临时 WebView 注入访问真实 IPC 和文件系统，并点击部分真实页面控件；并非每个系统对话框都逐一点击。首次测试中的 IPC 拦截与动画等待脚本问题已排除；最终测试没有拦截 IPC，也没有强制结束动画。临时 native 命令和注入入口已从产品代码移除。过程日志和脚本保存在 `/private/tmp/skill-studio-batch23-20260916/`。

## 数据核对与恢复

专用测试 skill、分组、项目、生成的事务文件已清理；Codex、Claude 和 Skill Studio 配置恢复测试前的原始字节。

收尾曾发现 Claude 的 `skill-yuque-cli-guide` 入口从原软链接变成了托管副本。副本记录时间为 03:44:25 UTC，晚于测试脚本结束时间 03:44:06 UTC；测试脚本没有对该 skill 发起注册操作，无法据此确定额外操作来源。核实原始来源目录的文件哈希与权限完整一致后，将该副本保留至临时证据目录，并恢复原软链接；新增 Finder 元数据和空事务锁也移至证据目录。

最终再次逐项核对所有原有 skill 入口、语雀各真实来源、目录结构、文件 SHA-256、权限和链接目标，以及三份配置：`protectedUnchanged=true`、`configsUnchanged=true`，无差异。没有创建 Git commit。

未进行 Windows/Linux 的真实桌面测试；进程中断恢复及失败回滚由隔离 Rust 测试覆盖。前端构建仍有既有的大包体积提示，不影响本轮构建通过。

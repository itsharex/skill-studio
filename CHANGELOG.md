# 更新日志

## v0.5.1 — 2026-09-25

- **在线 Skill 扫描**：排除不支持的 OpenClaw 专用目录，修复 Ponytail 等仓库因适配版本同名而无法安装的问题。
- **安装来源选择**：仓库仍有多个同名 Skill 时，展示候选路径、描述和完整目录内容是否一致，由用户选择安装版本；本机和远程 Hub 均支持。
- **安装保护**：记录所选仓库路径，拒绝无效候选及跨来源路径的同名覆盖；补充筛选、资源差异、取消和失败重试测试。

## v0.5.0 — 2026-09-24

- **MCP 分组启用**：修复 OpenCode、Pi、Grok 新部署的直连分组成员继承 Hub 停用状态，导致分组启用成功但服务仍停用的问题；保留 Hub 定义及已有手动停用条目的保护。

- **远程 MCP 展示**：MCP Hub 只读扫描 Linux 服务器上各 Agent 的已有配置，以卡片展示名称及所属 Agent；支持搜索、重新扫描和禁用 Agent 隐藏，不提供配置、网关或授权操作。

- **Agent 图标**：校准品牌图形的视觉尺寸，收紧 Pi 原始留白、略微缩小 OpenCode，并放大细线条的 Grok；导航、设置、Hub、项目及来源弹窗共用 20px／16px 尺寸规则，防止图标被布局挤窄，保持组件间距。

- **来源统计与设置体验**：关闭 Agent 时同步隐藏 Skill／MCP Hub 对应来源统计和专属未托管内容，列表总量与 token 同步更新；Hub、Agent、项目概览的统计条保持单行，组件使用自然宽度和原始间距，来源过多时横向滚动；切换应用保持按钮焦点和页面位置，减少重复扫描与 CLI 探测。

- **更多 MCP Agent**：新增 OpenCode v2、Pi（pi-mcp-adapter 扩展）与 Grok Build 的 MCP 扫描、直连接入、Studio 代理、分组和项目管理；保留 JSONC 注释与原生扩展字段，支持关闭管理时还原原配置。

- **窗口布局**：默认窗口扩大为 1360×820，最小尺寸设为 1140×620；搜索框收窄至最多 208px，顶部工具栏固定单行；Skill Hub 来源与统计保持对齐，避免新增 Agent 后导航错位。
- **更多 Skill Agent**：新增 OpenCode、Pi、Grok Build 的标准目录扫描、安装、托管还原、分组、项目部署和删除恢复。
- **目录与展示**：支持 OpenCode 的 XDG 与附加配置目录、Pi／Grok 的目录环境变量；修复多层配置目录覆盖，新增品牌图标与来源标记，项目页展示实际写入目录，共享目录不再误报 Agent 已安装。
- **分组边界**：新 Agent 暂不读取或改写原生启停配置，分组只撤回 Studio 部署的副本，手动 Skill 保留；界面使用“已安装”统计并说明保留策略范围。

## v0.4.1 — 2026-09-23

- **macOS 窗口生命周期**：按 `Cmd+W` 仅隐藏窗口，Skill Studio 继续在程序坞运行；重新激活 App 可恢复窗口。未保存修改仍会提示确认。
- **完整退出**：按 `Cmd+Q` 退出桌面 App 时同时请求停止独立运行的 MCP 网关，避免留下后台进程。

## v0.4.0 — 2026-09-23

- **MCP 管理开关**：支持关闭全部 MCP 管理入口，仅保留 Skill 管理；关闭时还原 Codex、Claude Code 的原有接入，重新开启时恢复托管，并保护外部修改。异常退出后自动恢复事务并校正开关状态。
- **官方 MCP 目录**：支持搜索和分页浏览官方 Registry，将可用连接填入安装表单；需要额外凭据或参数的服务保留手动配置提示。
- **连接诊断**：Agent 直连服务也可测试握手和工具列表，展示最近测试结果并区分登录需求与无效 Token；SSE 服务明确提示在 Agent 中验证。
- **配置恢复**：提供 MCP 配置备份列表，按事务恢复原生配置与托管记录；检测后续修改并停止覆盖。支持一次移除项目中的全部托管接入，保留手动配置和 Hub 服务。
- **界面与性能**：区分设置、安装和管理操作的图标，统一 MCP 状态缓存与轮询间隔，减少切换页面时的重复扫描。
- **发布与验证**：固定 Rust 工具链，统一等待 CI、发布和最终核验流程；新增可选的 SSH helper 与 NFS 验证脚本。

## v0.3.3 — 2026-09-22

- **MCP 安装与管理**：统一 Hub 保存、托管和还原流程，使用紧凑卡片管理来源与接入；支持在 Agent 和项目页面添加 MCP。
- **授权体验**：Studio 网关自动识别授权需求，网页登录完成后通知应用返回并测试连接，共享授权供多个 Agent 使用。
- **分组行为**：MCP 分组保存仅记录成员引用，不再隐式托管；复用已有配置，停用只撤回分组部署，并保护手动停用及外部修改。
- **导航与交互**：Agent 和项目内容跟随 Skill Hub / MCP Hub 类型，统一分组入口与操作；增加 Hub 切换过渡，并缓存 Agent 共享数据以减少切换抖动。
- **内置服务保护**：识别 Codex App 内部 MCP，默认隐藏并禁止修改；设置中可开启灰色只读卡片展示。

## v0.3.2 — 2026-09-18

- **远程管理开关**：在服务器页新增桌面端服务器连接开关；关闭后保留服务器页签和已保存连接，仅隐藏主页目标选择器与连接配置，并安全切回本机管理。
- **设置界面**：将相关区域整理为“远程管理”和“服务器设置”，目标说明在选择器显示状态变化时平滑调整位置，并遵循系统的减少动态效果偏好。
- **回归验证**：覆盖开关偏好持久化、连接记录保留、远程目标断开回本机、失败恢复及管理操作期间禁用开关等场景。

## v0.3.1 — 2026-09-18

- **文件事务与路径安全**：修复软链接配置写入、父级路径规范化和中断事务恢复问题；跨 Agent、分组与项目的批量变更共用同一事务，失败时完整回滚。
- **托管与缓存一致性**：加强手动 skill、Hub 副本及本地修改的保护，修复管理操作、设置恢复后的缓存刷新，并移除未使用的旧组件和接口。
- **远程上传验证**：补充分块上传的真实 SSH 回归，校验跨多个 64 KiB 分块及非对齐尾块的二进制内容完整性。
- **界面文案**：将手动安装状态统一为“未托管”，Hub 托管状态统一为蓝色“托管中”徽标。

## v0.3.0 — 2026-09-17

- **远程服务器管理**：通过 SSH 连接 Linux 服务器，在同一套界面中管理远程 Agent、Skill Hub、分组、项目、设置与备份；切换目标时完整隔离查询缓存和编辑状态。
- **SSH 配置接入**：直接读取系统 SSH config 供用户搜索和选择，沿用其中的用户名、端口、密钥及跳板机设置，并支持密码与主机指纹交互。
- **远程辅助程序**：桌面端自动部署带版本校验的轻量辅助程序，通过受限 JSON 协议复用现有管理服务，支持远程目录浏览和从本机上传 Skill。
- **连接体验**：服务器名称以绿色或红色显示在线状态，设置、刷新和目标选择集中在顶部；断线后可明确重连，远程错误不会回退到本机操作。
- **发布构建**：安装包自动携带 x86_64 与 ARM64 Linux 辅助程序，并在主分支 CI 中提前验证辅助程序的发布构建。

## v0.2.0 — 2026-09-17

- **项目本地 skill**：扫描项目中的真实 skill 文件，显示尚未托管数量；托管时保留项目原件，并支持逐个启用或停用。
- **统一文件管理**：Hub、Agent 与项目共用真实文件删除流程；普通目录删除前创建备份，支持恢复或彻底删除，软链接直接移除且不生成无意义备份。
- **兼容迁移**：自动迁移旧版项目备份记录，延续已有恢复能力。
- **内容预览**：卡片可直接预览 `SKILL.md`，显示正文 token 粗估，并调整图标与操作区域布局。
- **开发体验**：Debug 构建使用独立应用名称，避免与正式版混淆。
- **发布流程**：新增版本同步、发布门禁、精确提交 CI 校验、五平台安装包校验和 Homebrew 更新验证。

## v0.1.2 — 2026-09-17

- **手动 skill 策略**：新增手动安装页签与标记，保留开关即时生效；无启用分组时也会暂时停用组外 skill，重新开启仅恢复由策略停用的项。
- **应用管理**：支持在设置中开启或退出 Agent 管理，退出时还原原目录并保留 Hub 独立副本；修复重新开启后重复计数和误报占用。
- **项目卡片**：新增各 Agent 的项目统计、筛选、拖拽排序与启停，展示分组及 skill 启用数量；停用保留绑定和手动文件，遇到本地修改时阻止移除。
- **Token 展示**：Hub 卡片突出 SKILL.md 粗估，附带文件和目录合计放入悬停详情。
- **交互修复**：统一添加入口，修复分组操作时状态闪烁及弹窗从右下角偏移的动画。

## v0.1.1 — 2026-09-17

- **Token 估算**：新增 skill 正文与文本附带文件的体量估算，在 Agent、分组和项目中显示汇总，提示副本漂移带来的偏差。
- **Hub 筛选**：新增已托管数量和筛选，统一来源计数口径，托管或还原后保持结果可见。
- **Agent 统计**：展示已启用分组和 skill 数量，排除停用及不可用项。
- **扫描优化**：短时间内复用扫描结果，跳过指向 FIFO、设备节点的软链，避免 token 估算阻塞扫描。

## v0.1.0 — 2026-09-16

Skill Studio 首个公开版本，用于集中管理 skill，并为 Claude Code、Codex 和项目配置 skill 组合。

- **Skill Hub**：汇总多个来源的 skill、合并相同内容的卡片，按原始来源统计和筛选。
- **安装与导入**：支持 skills.sh 搜索安装和本地目录导入，统一存放到 `~/.skill-studio`。
- **托管与还原**：保留原始来源、备份和托管记录，支持移出 Hub 时还原。
- **Agent 分组**：支持创建、编辑、拖拽排序、启用与停用，以及手动 skill 的保留/暂时停用策略。
- **项目配置**：选择 Agent、分组和 skill 后显式写入，未保存离开时提示；文件与配置事务提交，保护手动文件和本地修改。
- **设置**：支持主题、目录、备份恢复及维护操作，完善错误提示和目录边界校验。
- **界面**：顶部导航、独立卡片、悬停操作，统一应用图标与图标尺寸。

### 使用说明

- “不保留手动 skill”在启用、切换或重新应用分组时生效；停用分组后恢复手动 skill，不删除文件。
- 历史项目文件未记录部署归属时不会自动清除；重新写入后才纳入管理。
- 不支持自动迁移非空 Hub 目录，也不支持自动检查更新。

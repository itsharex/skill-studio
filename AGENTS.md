# 开发约定

- 可参考 GitHub 相关项目，优先官方库和方案，但以用户需求为准。

## 图标

- 导航及设置页图标为 20×20（`h-5 w-5`），Hub 来源统计、来源标记及操作图标为 16×16（`h-4 w-4`），关于页应用展示图标保留 48×48，Agent 保留各自品牌图标。

## 发布

- 用户明确要求发布时，由当前代理直接执行 [.agents/skills/skill-studio-release/SKILL.md](.agents/skills/skill-studio-release/SKILL.md)，不固定模型、不自动委派发布子代理。普通提交、push、发布讨论或修改发布规则不触发发布。
- 发布流程、检查复用、失败处理只维护在该 Skill 中；沿用当前任务的变更与验证记录，避免重新调研和重复检查。
- 发布前说明目标版本，进行中报告有实际信息的阶段进展，完成后简报版本、提交、Release 链接和验证结果。
